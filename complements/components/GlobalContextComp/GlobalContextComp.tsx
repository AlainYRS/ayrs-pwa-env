import { createContext } from "react"; //Import the createContext function from the react library

const GlobalContext = createContext({}); //Create the context

export default GlobalContext; //Export the context